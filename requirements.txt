# ============================================================================
# requirements.txt · EntroDiff (MVP 阶段)
# ----------------------------------------------------------------------------
# 仅包含 MVP 代码实际 import 的运行时依赖。
# 后续阶段依赖（clawpack / diffusers / einops / POT / hydra 等）
# 见 pyproject.toml [project.optional-dependencies]。
# ============================================================================

# --- 数值核心 ---
numpy>=1.24,<2.0
scipy>=1.11,<2.0

# --- 深度学习 ---
torch>=2.2,<3.0

# --- 实验配置 / IO ---
PyYAML>=6.0,<7.0

# --- 可视化 ---
matplotlib>=3.8,<4.0
tqdm>=4.66,<5.0
